const recipesContainer=document.querySelector("#recipes");const recipeTemplate=document.querySelector("#recipe-card-template");const recipes=[{title:"Pancakes",category:"Breakfast",timeMinutes:20,difficulty:"Easy",image:"https://www.inspiredtaste.net/wp-content/uploads/2025/07/Pancake-Recipe-1.jpg",ingredients:["1 cup flour","1 cup milk","1 egg","1 tbsp sugar","1 tsp baking powder","Pinch of salt"],steps:["Mix the flour, sugar, baking powder, and salt in a bowl.","Add the milk and egg, then mix until smooth.","Heat a pan on medium heat.","Pour a small amount of batter into the pan.","Cook until bubbles appear, then flip.","Cook the other side until golden."],comment:"Simple fluffy pancakes, perfect for breakfast with honey, fruit, or syrup."}];function addListItems(listElement,items){items.forEach(item=>{const listItem=document.createElement("li");listItem.textContent=item;listElement.append(listItem)})}function createRecipeCard(recipe){const card=recipeTemplate.content.cloneNode(true);const meta=`${recipe.timeMinutes} min · ${recipe.difficulty}`;card.querySelectorAll(".recipe-title").forEach(element=>element.textContent=recipe.title);card.querySelectorAll(".recipe-category").forEach(element=>element.textContent=recipe.category);card.querySelectorAll(".recipe-meta").forEach(element=>element.textContent=meta);const image=card.querySelector(".recipe-image");image.src=recipe.image;image.alt=recipe.title;addListItems(card.querySelector(".recipe-ingredients"),recipe.ingredients);addListItems(card.querySelector(".recipe-steps"),recipe.steps);card.querySelector(".recipe-comment").textContent=`Comment: ${recipe.comment}`;return card}function renderRecipes(recipeList){recipesContainer.replaceChildren();recipeList.forEach(recipe=>recipesContainer.append(createRecipeCard(recipe)))}renderRecipes(recipes);